const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('open')}));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.focus()}});const descriptions=['Ogni forma comincia da una traccia.','Le linee trovano il loro equilibrio.','Il contrasto rivela ogni dettaglio.'];document.querySelectorAll('.stages button').forEach(b=>b.addEventListener('click',()=>{document.querySelector('.artwork').dataset.stage=b.dataset.stage;document.querySelectorAll('.stages button').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));document.querySelector('.stage-description').textContent=descriptions[Number(b.dataset.stage)]}));const dialog=document.querySelector('dialog');document.querySelectorAll('.concept-card').forEach(b=>b.addEventListener('click',()=>{dialog.dataset.view=b.dataset.view;dialog.showModal()}));document.querySelector('.close-dialog').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
